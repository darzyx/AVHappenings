import React from 'react';
import { Icon } from 'semantic-ui-react';

const Loading = () => (
  <div className='loading-page'><Icon loading name='spinner' size='big' /></div>
)

export default Loading;
